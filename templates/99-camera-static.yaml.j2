# Camera interfaces: static IPs (no internet). Generated by app-deploy playbook.
# Other interfaces remain from 50-cloud-init.yaml (DHCP).
network:
  version: 2
  ethernets:
{% for c in camera_static_config %}
    {{ c.name }}:
      dhcp4: false
      addresses:
        - {{ c.address }}
{% endfor %}
