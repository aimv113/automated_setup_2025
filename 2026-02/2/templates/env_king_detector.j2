# Generated by app-deploy playbook (fallback when admin/env-file.md missing).
# Machine/site identifier (edit as needed)
STATUS_MESSAGE={{ machine_name }}

# Models directory (post-reboot-verify creates ~/data; models go in ~/data/models)
MODEL_DIR={{ models_path }}

# InfluxDB (fill in after deploy)
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=
INFLUXDB_ORG=
INFLUXDB_BUCKET=

# Camera / RTSP (fill in after deploy)
# VIDEO_SOURCE=rtsp://user:pass@192.168.1.100:554/stream
# CAMERA_IP=192.168.1.100

# GPU (defaults)
PRIMARY_DETECTOR_GPU=cuda:0
SECONDARY_DETECTOR_GPU=cuda:1

# Edit and add other keys from king_detector config.py / README as needed.
